---
title: "Chapter 1: 주방을 짓다 — 하이퍼바이저의 세계"
date: 2026-04-01 09:00:00 +0900
categories: [NCP, NCE]
tags: [ncp, nce, 자격증, hypervisor, xen, kvm, g1, g2, g3, 가상화]
description: "NCP NCE 자격증 대비 학습 자료 - XEN(G1/G2)과 KVM(G3) 하이퍼바이저 차이 및 OS 스토리지 규칙"
---

> **학습 목표**: XEN(G1/G2)과 KVM(G3)의 차이, CPU/Memory Over Commit 규칙, OS 영역 스토리지 차이를 완벽히 구분한다.

---

## 이야기: 첫 번째 서버 앞에서 멈추다

민준은 NCP 콘솔을 열고 **[서버 생성]** 버튼을 눌렀다.
그런데 첫 화면부터 모르는 말이 가득했다.

> "G1, G2, G3... 세대가 뭐야?  
> XEN? KVM? 하이퍼바이저는 또 뭐야?"

지수가 옆에서 설명을 시작했다.

> "쉽게 말하면, 물리 서버 한 대 위에 여러 가상 서버를 올리는 기술이야.  
> 그 '가상화 관리자 프로그램'이 바로 하이퍼바이저야.  
> 마치 하나의 건물 안에 여러 개의 독립된 가게를 만드는 것처럼."

> "그러면 G1/G2/G3은요?"

> "세대별로 하이퍼바이저가 달라. NCP는 세대에 따라 두 종류를 사용해."

---

## 🔑 핵심 개념 1: 서버 세대와 하이퍼바이저

**하이퍼바이저(Hypervisor)** = 물리 서버의 CPU, Memory, Storage를 쪼개서 여러 가상 서버(VM)에 나눠주는 가상화 관리 소프트웨어.

| 구분 | **1세대(G1) / 2세대(G2)** | **3세대(G3)** |
|------|--------------------------|--------------|
| **하이퍼바이저** | **XEN** | **KVM** |
| CPU 성능 | 기본 | **비약적으로 향상** (KVM 전환 이유) |
| AMD 기반 서버 | ❌ 지원 안 함 | ✅ 지원 (High CPU, High Memory, Standard) |
| 지원 리전 | 전체 리전 | 한국, 싱가포르, 일본 **(VPC 환경만)** |

> ⚠️ **시험 포인트 #1**  
> **하이퍼바이저가 다른 리소스 간에는 호환 불가!**  
> XEN으로 만든 서버 이미지를 KVM 서버에서 사용할 수 없다.

```
[호환 불가 예시]
G2 서버의 내 서버 이미지 → G3 서버에서 사용 ❌
G3 서버의 내 서버 이미지 → G2 서버에서 사용 ❌
같은 세대끼리만 가능 ✅
```

---

## 🔑 핵심 개념 2: CPU vs Memory 할당 규칙

민준이 물었다.

> "물리 서버 CPU가 8코어면, 가상 서버에 총 8코어만 줄 수 있어요?  
> 100개 서버 만들면 CPU는 어떻게 되는 거야?"

지수의 답:

> "CPU는 **Over Commit**이 허용돼.  
> 실제보다 더 많이 약속해줄 수 있어. 어차피 다 동시에 100% 쓰는 경우는 드물거든.  
> 근데 **메모리는 안 돼**. 메모리는 실제로 있는 만큼만 줄 수 있어."

| 자원 | 할당 단위 | **Over Commit** | 스펙 변경 |
|------|-----------|----------------|-----------|
| **CPU** | vCPU | **허용 ✅** | 가능 |
| **Memory** | GB | **불허 ❌** | 가능 |

> 💡 **비유**: CPU는 "자리 예약"처럼 오버부킹 가능. Memory는 실제 물리 공간이라 있는 만큼만.

---

## 🔑 핵심 개념 3: 하이퍼바이저별 OS 스토리지 차이 (⭐ 매우 중요!)

가장 많이 헷갈리는 부분. OS가 설치되는 **부팅 디스크(OS 영역)**의 규칙이 세대마다 완전히 다르다.

| 구분 | **XEN (G1/G2)** | **KVM (G3)** |
|------|----------------|--------------|
| **Linux OS 영역** | **50GB 고정** | 최소 **10GB** (선택 가능) |
| **Windows OS 영역** | **100GB 고정** | 최소 **30GB** (선택 가능) |
| **OS 영역 확장** | **불가** ❌ | **서버 정지 후 확장 가능** ✅ |
| **OS 영역 축소** | **불가** ❌ | **불가** ❌ |

> ⚠️ **시험 포인트 #2**  
> XEN: OS 영역 **확장도 축소도 불가**. 처음부터 고정!  
> KVM: OS 영역 **확장 가능** (단, 서버를 반드시 **정지**한 후에만)

```
[민준의 상황]
G1 서버로 시작 → OS 디스크 50GB 꽉 참 → 늘리고 싶음
→ XEN이라 불가능! ❌ → G3(KVM)으로 마이그레이션 필요

G3 서버로 시작 → OS 디스크 10GB 꽉 참 → 늘리고 싶음
→ 서버 정지 → 확장 가능! ✅
```

---

## 📊 전체 비교 요약

```
        XEN (G1/G2)                    KVM (G3)
        ──────────                     ────────
하이퍼바이저: XEN               하이퍼바이저: KVM
AMD 서버: ❌                    AMD 서버: ✅
OS 크기: 고정 (L:50G / W:100G) OS 크기: 가변 (L:10G~ / W:30G~)
OS 확장: ❌                     OS 확장: ✅ (정지 후)
추가 스토리지: 최대 2TB × 15개  추가 스토리지: 최대 16TB × 20개
```

*(추가 스토리지 제한은 Chapter 4에서 자세히 다룸)*

---

## 📝 시험 대비 체크리스트

- [ ] G1/G2 = **XEN** 하이퍼바이저
- [ ] G3 = **KVM** 하이퍼바이저
- [ ] 서로 다른 하이퍼바이저 간 **호환 불가**
- [ ] CPU: **Over Commit 허용** ✅
- [ ] Memory: **Over Commit 불허** ❌
- [ ] XEN OS 영역: Linux **50GB** / Windows **100GB** — **확장/축소 모두 불가**
- [ ] KVM OS 영역: Linux 최소 **10GB** / Windows 최소 **30GB** — **서버 정지 후 확장 가능**
- [ ] G3는 **VPC 환경**에서만 지원 (Classic 환경 불가)

---

## 🧠 암기 핵심 문장

> "**XEN은 태어날 때부터 크기가 정해진다. KVM은 나중에 리모델링이 가능하다.**"  
> "**CPU는 오버부킹 가능, 메모리는 실제 있는 만큼만.**"

---

*다음 챕터: `02_서버타입.md` → 어떤 서버를 골라야 할까?*
